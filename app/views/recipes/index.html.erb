<div class="mx-auto p-6">
  <% if notice %>
    <div class="mb-4 p-4 bg-green-100 border border-green-400 text-green-700 rounded">
      <%= notice %>
    </div>
  <% end %>

  <div class="mb-6 text-center">
    <h1 class="text-4xl font-bold text-gray-800">Recipes</h1>
  </div>

  <% if @recipes.any? %>
    <div id="recipes" class="flex flex-wrap gap-10 justify-center">
      <% @recipes.each do |recipe| %>
        <div class="bg-white rounded-lg shadow-lg overflow-hidden hover:shadow-xl transition-shadow duration-300 w-full md:w-1/2 lg:w-1/4">
          <img src="<%= recipe.image %>" alt="<%= recipe.title %>" class="w-full h-48 object-cover">
          <div class="p-4">
            <h2 class="text-lg font-semibold text-gray-800 truncate">
              <%= recipe.title %>
            </h2>
            <div class="text-right">
              <p class="mb-4">Ingredients: <%= recipe.ingredients.length %></p>
              <%= link_to "Show this recipe", recipe, class: "bg-blue-500 text-white text-sm px-4 py-2 rounded hover:bg-blue-600" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="mt-6 text-center">
      <p class="text-gray-500 text-lg">No recipes available.</p>
    </div>
  <% end %>
</div>
